source "https://rubygems.org"

# This Gemfile uses the github-pages meta-gem so local builds match
# what GitHub Pages produces in production.
gem "github-pages", group: :jekyll_plugins

# If you ever migrate off the GH Pages gem to a custom Actions build,
# replace the line above with `gem "jekyll", "~> 4.3"` and uncomment
# the explicit plugin block below.
#
# group :jekyll_plugins do
#   gem "jekyll-feed"
#   gem "jekyll-seo-tag"
#   gem "jekyll-sitemap"
# end

# Performance booster on Linux/macOS
gem "wdm", "~> 0.1.1", :install_if => Gem.win_platform?
